#ifndef CORE_TYPES_VAULT_TYPES_H
#define CORE_TYPES_VAULT_TYPES_H

#include <string>

/**
 * Shared data types for vault entries will go here
 * (password records, notes, metadata...)
 *
 * We'll define structs like PasswordEntry later.
 */
struct PlaceholderVaultTypes {
    std::string id;
    std::string note;
};

#endif //CORE_TYPES_VAULT_TYPES_H
